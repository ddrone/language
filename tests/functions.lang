fun fib(x: int): int {
    if (x <= 1) {
        return x;
    }
    return fib(x - 1) + fib(x - 2);
}

fun factorial(x: int): int {
    if (x <= 1) {
        return 1;
    }
    return debug x * factorial(x - 1);
}

fun returnList(): list<int> {
    return [1, 2, 3];
}

fun listTest(): int {
    val ls = returnList();
    debug get(ls, 1);
    debug ls;
    return 0;
}

fun main(): int {
    listTest();
    val v = spawn factorial(5);
    debug v;
    return fib(10);
}
